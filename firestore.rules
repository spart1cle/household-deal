rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /households/{code} {
      allow read, write: if request.auth != null && code.matches('[A-Z0-9]{4,12}');
    }
  }
}
